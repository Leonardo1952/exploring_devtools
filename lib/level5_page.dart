import 'package:flutter/material.dart';

class Level5Page extends StatelessWidget {
  const Level5Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Level 5')),
      body: Center(),
    );
  }
}
